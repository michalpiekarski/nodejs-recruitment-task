const movie = require('./movie');
const movie_query = require('./movie_query');
const comment = require('./comment');

module.exports = {
  movie: movie,
  movie_query: movie_query,
  comment: comment,
};
